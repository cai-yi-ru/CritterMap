## 1. 台北市特寵醫院資料整理

- [x] 1.1 審查 TaipeiHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 1.2 統一台北市特寵醫院 `typeText` 為標準用語
- [x] 1.3 補全台北市特寵醫院 `reservationRequired`、`hasEmergencyService`、`nightClinic` 欄位
- [x] 1.4 修正台北市特寵醫院 `business_hours` 格式
- [x] 1.5 補填台北市特寵醫院缺少的 `last_checked` 欄位

## 2. 新北市特寵醫院資料整理

- [x] 2.1 審查 NewTaipeiHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 2.2 統一新北市特寵醫院 `typeText` 為標準用語
- [x] 2.3 補全新北市特寵醫院 `reservationRequired`、`hasEmergencyService`、`nightClinic` 欄位
- [x] 2.4 修正新北市特寵醫院 `business_hours` 格式
- [x] 2.5 補填新北市特寵醫院缺少的 `last_checked` 欄位

## 3. 基隆市特寵醫院資料整理

- [x] 3.1 審查 KeelungHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 3.2 統一基隆市特寵醫院 `typeText` 並補全布林欄位
- [x] 3.3 修正基隆市特寵醫院 `business_hours` 格式並補填 `last_checked`

## 4. 桃園市特寵醫院資料整理

- [x] 4.1 審查 TaoyuanHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 4.2 統一桃園市特寵醫院 `typeText` 並補全布林欄位
- [x] 4.3 修正桃園市特寵醫院 `business_hours` 格式並補填 `last_checked`

## 5. 新竹市、新竹縣特寵醫院資料整理

- [x] 5.1 審查 HsinchuCityHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 5.2 統一新竹市特寵醫院 `typeText` 並補全布林欄位，補填 `last_checked`
- [x] 5.3 審查 HsinchuCountyHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 5.4 統一新竹縣特寵醫院 `typeText` 並補全布林欄位，補填 `last_checked`

## 6. 苗栗縣特寵醫院資料整理

- [x] 6.1 審查 MiaoliHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 6.2 統一苗栗縣特寵醫院 `typeText` 並補全布林欄位
- [x] 6.3 修正苗栗縣特寵醫院 `business_hours` 格式並補填 `last_checked`

## 7. 台中市特寵醫院資料整理

- [x] 7.1 審查 TaichungHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 7.2 統一台中市特寵醫院 `typeText` 為標準用語
- [x] 7.3 補全台中市特寵醫院 `reservationRequired`、`hasEmergencyService`、`nightClinic` 欄位
- [x] 7.4 修正台中市特寵醫院 `business_hours` 格式
- [x] 7.5 補填台中市特寵醫院缺少的 `last_checked` 欄位

## 8. 彰化縣特寵醫院資料整理

- [x] 8.1 審查 ChanghuaHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 8.2 統一彰化縣特寵醫院 `typeText` 並補全布林欄位
- [x] 8.3 修正彰化縣特寵醫院 `business_hours` 格式並補填 `last_checked`

## 9. 嘉義市特寵醫院資料整理

- [x] 9.1 審查 ChiayiCityHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 9.2 統一嘉義市特寵醫院 `typeText` 並補全布林欄位
- [x] 9.3 修正嘉義市特寵醫院 `business_hours` 格式並補填 `last_checked`

## 10. 台南市特寵醫院資料整理

- [x] 10.1 審查 TainanHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 10.2 統一台南市特寵醫院 `typeText` 為標準用語
- [x] 10.3 補全台南市特寵醫院 `reservationRequired`、`hasEmergencyService`、`nightClinic` 欄位
- [x] 10.4 修正台南市特寵醫院 `business_hours` 格式
- [x] 10.5 補填台南市特寵醫院缺少的 `last_checked` 欄位

## 11. 高雄市特寵醫院資料整理

- [x] 11.1 審查 KaohsiungHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 11.2 統一高雄市特寵醫院 `typeText` 為標準用語
- [x] 11.3 補全高雄市特寵醫院 `reservationRequired`、`hasEmergencyService`、`nightClinic` 欄位
- [x] 11.4 修正高雄市特寵醫院 `business_hours` 格式
- [x] 11.5 補填高雄市特寵醫院缺少的 `last_checked` 欄位

## 12. 屏東縣特寵醫院資料整理

- [x] 12.1 審查 PingtungHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 12.2 統一屏東縣特寵醫院 `typeText` 並補全布林欄位
- [x] 12.3 修正屏東縣特寵醫院 `business_hours` 格式並補填 `last_checked`

## 13. 宜蘭縣特寵醫院資料整理

- [x] 13.1 審查 YilanHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 13.2 統一宜蘭縣特寵醫院 `typeText` 並補全布林欄位
- [x] 13.3 修正宜蘭縣特寵醫院 `business_hours` 格式並補填 `last_checked`

## 14. 台東縣特寵醫院資料整理

- [x] 14.1 審查 TaitungHospitalList.ts 中所有特寵醫院，補全 `pet_category_group` 欄位
- [x] 14.2 統一台東縣特寵醫院 `typeText` 並補全布林欄位
- [x] 14.3 修正台東縣特寵醫院 `business_hours` 格式並補填 `last_checked`
